# Versiones verificadas contra Python 3.14.7 en Windows (2026-09).
# Los pines anteriores (Flask 3.0.3 / SQLAlchemy 2.0.32 / psycopg 3.2.1) son
# previos a Python 3.14 y no tienen wheels para cp314: psycopg-binary fallaba
# al instalar. Si se cambia la version de Python, revisar estos pines.

# --- Núcleo ---
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Login==0.6.3
Flask-WTF==1.3.0
WTForms==3.2.2
python-dotenv==1.2.3
email-validator==2.3.0

# --- Base de datos ---
SQLAlchemy==2.0.52
psycopg[binary]==3.3.5

# --- PDF de remisiones ---
# WeasyPrint da mejor calidad (usa el mismo HTML/CSS de las plantillas) pero en
# Windows requiere instalar GTK aparte. ReportLab no necesita nada extra.
reportlab==5.0.1

# --- Producción (Render/Railway, Linux) ---
gunicorn==26.2.0
